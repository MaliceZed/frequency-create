package maze.frequency.item;

/**
 * Marker interface for all symbol items (regular, andesite, copper, fluid, etc.).
 * Used by render mixins to apply custom display contexts (SURFACE, REDSTONE_LINK, TABLE_CLOTH).
 */
public interface ISymbolItem {
}
